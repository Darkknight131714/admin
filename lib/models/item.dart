class Item {
  String id, shopId, productId, price, quantity, custId;
  Item(
      {required this.custId,
      required this.id,
      required this.price,
      required this.productId,
      required this.quantity,
      required this.shopId});
}
